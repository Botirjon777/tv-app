# Dependencies & build output
node_modules/
dist/

# Local media uploads (use a volume in prod)
uploads/

# Secrets — never commit real env files. Example templates stay tracked.
.env
.env.*
!.env.example
!.env.*.example
